skill_reco:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: "skill_reco.duckdb"
      extensions:
        - delta
        - httpfs

    prod:
      type: duckdb
      path: "/opt/airflow/skill_reco.duckdb"
      extensions:
        - delta
        - httpfs
